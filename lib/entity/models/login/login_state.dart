/// login statue enum
enum UserLoggingStatus {
  /// login status is unknown
  unknown,

  /// user is logged in
  logged,

  /// user is not logged
  notLogged,

  /// error occurred thile logging
  loginError,
}
